RSpec.describe "Math" do
  it "adds numbers" do
    expect(1 + 1).to eq(2)
  end

  it "subtracts numbers" do
    expect(10 - 3).to eq(7)
  end

  it "multiplies numbers" do
    expect(4 * 5).to eq(20)
  end
end
